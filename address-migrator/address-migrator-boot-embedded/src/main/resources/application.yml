#Server
server:
  port: 9009
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: address-migrator
  mvc:
    servlet:
      path: /addressMigrator
  batch:
    lightmin:
      client:
        external-links:
          hystrix: http://localhost:${server.port}/hystrixdashboard
          batchtasks: http://localhost:${server.port}/batchtasks
    job:
      enabled: false

